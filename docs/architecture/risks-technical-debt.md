---
status: 현재
owners: [김용준]
last_reviewed: 2026-09-13
---

# 위험과 기술 부채

## 문서 목적

현재 아키텍처에 남아 있는 주요 위험, 알려진 한계와 의도적으로 수용한 기술 부채를 추적합니다.

## 언제 수정하는가

- 새로운 구조적 위험이나 기술 부채를 발견할 때
- 영향, 우선순위, 완화 계획 또는 담당자가 바뀔 때
- 위험이 해소되거나 현실화될 때

## 처리 신뢰성

| 항목 | 영향 | 대응 | 담당자 |
|---|---|---|---|
| 처리 중 예외가 재시도·DLQ 경로를 타지 않음 | 역할 핸들러가 `(False, 사유)`를 반환하지 않고 예외를 던지면 배치가 커밋되지 않아 같은 메시지를 무한 재소비. 파티션 정지 | 메시지 처리 전체를 예외 경계로 감싸 DLQ 판정에 포함 | 미정 |
| 역직렬화 실패 시 워커 종료 | 잘못된 JSON이 소비 루프 밖으로 예외를 전파해 프로세스가 종료되고 재시작 후 같은 오프셋에서 반복 | 원문 바이트로 소비하고 파싱 실패를 즉시 DLQ 처리 | 미정 |
| 오프셋 커밋 단위가 폴링 배치 전체 | 한 건 실패가 최대 64건 재처리를 유발. 잠금이 중복 완료는 막지만 불필요한 재작업 발생 | 파티션·오프셋 단위 커밋으로 축소 | 미정 |
| 잠금 경합이 재시도 횟수를 소모 | 정상 처리 중인 Job의 중복 메시지가 `RETRY_MAX_COUNT` 초과로 DLQ에 적재되어 DLQ 신호의 신뢰도 저하 | 잠금 경합을 일반 실패와 분리해 별도 경로로 처리 | 미정 |
| 재시도 백오프를 소비 루프 안에서 대기 | 최대 `RETRY_BACKOFF_MAX_SECONDS` 동안 커밋과 폴링이 멈춤. 실패가 몰리면 리밸런스 위험 | 지연 토픽 또는 예약 시각 헤더로 이전 | 미정 |
| downstream 처리에 처리 잠금 없음 | downstream 워커를 2개 이상으로 확장하면 같은 Job을 동시에 처리할 수 있음 | 추론 경로와 동일한 `idem:job:*` 잠금 적용 | 미정 |

## 데이터 정합성

| 항목 | 영향 | 대응 | 담당자 |
|---|---|---|---|
| 데이터베이스 커밋과 Kafka 발행이 분리 | 발행 실패 시 Job이 `PENDING`에 영구 고착. 같은 멱등성 키로 재요청하면 고착된 Job을 정상 접수로 반환 | Transactional Outbox 도입 또는 `PENDING` 정리 작업 추가. `job_events`가 아웃박스 기반으로 활용 가능 | 미정 |
| `jobs.retry_count` 미사용 | 항상 0으로 기록되어 재시도 이력을 데이터베이스에서 확인 불가 | 헤더의 `retry-count`를 반영하거나 컬럼 제거 | 미정 |
| 보존 정책 부재 | `jobs`와 `job_events`가 무기한 증가 | 보존 기간과 아카이브 정책 정의 | 미정 |

## 관측 가능성

| 항목 | 영향 | 대응 | 담당자 |
|---|---|---|---|
| HTTP 지표 라벨에 원본 경로 사용 | `GET /jobs/{job_id}` 호출마다 새 시계열이 생성되어 Prometheus 메모리 증가 | 라우트 템플릿을 라벨로 사용 | 미정 |
| 이벤트 `trace_id`가 추적 ID와 무관 | 로그·데이터베이스에서 Jaeger 추적으로 연결 불가 | Envelope 생성 시 활성 스팬의 추적 ID 사용 | 미정 |
| 로그가 구조화 형식이 아님 | `job_id` 기준 Kibana 검색이 어려움 | JSON 포매터와 공통 필드 도입 | 미정 |
| 워커 readiness가 메트릭 포트 TCP 확인 | 소비 루프가 멈춰도 Ready로 보고됨 | 마지막 폴링 시각 기반 상태 확인으로 교체 | 미정 |

## 운영과 보안

| 항목 | 영향 | 대응 | 담당자 |
|---|---|---|---|
| 종료 신호 처리 없음 | SIGTERM 수신 시 즉시 종료되어 진행 중이던 재시도·DLQ 발행이 유실 | 신호 처리와 `terminationGracePeriodSeconds` 설정 | 미정 |
| 컨테이너가 root로 실행 | 권한 상승 위험 | 비루트 사용자와 `securityContext` 적용 | 미정 |
| NetworkPolicy와 PodDisruptionBudget 부재 | 네트워크 격리 없음. 노드 작업 중 가용성 보장 없음 | 기본 차단 정책과 PDB 추가 | 미정 |
| 상태 저장소 단일 인스턴스 | PostgreSQL·Redis·Kafka 장애 시 전체 중단 | 현재 환경에서는 **의도적으로 수용**. 운영 전환 시 재검토 | 김용준 |
| `/health/ready`에 타임아웃 없음 | 의존성 지연 시 프로브 커넥션 누적 | 개별 점검에 타임아웃과 짧은 캐시 적용 | 미정 |

## 미사용 자산

| 항목 | 내용 | 대응 |
|---|---|---|
| `inference-topic` | `KAFKA_INFERENCE_TOPIC`이 설정에 존재하고 토픽도 자동 생성되지만 어떤 역할도 발행·소비하지 않음 | 사용하거나 설정에서 제거 |
| 관측 설정 중복 | `deploy/observability/`, `deploy/k8s/overlays/observability/configs/`, `deploy/prometheus.yml`에 유사 설정이 병존 | 기준 위치를 하나로 정하고 나머지는 참조 |

## 수용한 부채

- **GPU 시뮬레이터**: 실제 추론 대신 지연·동시성·실패율을 재현합니다. 처리 특성 실험이 목적이므로 의도된 선택입니다. [ADR-0006](../decisions/ADR-0006-gpu-inference-simulator.md)
- **downstream 더미 처리**: 후단 처리는 지연만 재현하며 실제 저장·업로드·알림을 수행하지 않습니다.
- **B 모드의 처리 경로**: B 모드는 A 모드와 동일한 서비스를 사용하며 배포 단위와 관측 라벨만 분리됩니다.

## 관련 문서

- [제약 사항](constraints.md)
- [품질](quality.md)
- [실행 흐름](runtime.md)
- [장애 대응](../operations/incident-response.md)
